name: Build

on:
  push:
  pull_request:

jobs:
  build-and-test:
    runs-on: macos-14

    steps:
      - uses: actions/checkout@v4

      - name: Swift tests
        run: swift test

      - name: Xcode build
        run: xcodebuild -scheme Transcriptor -destination 'platform=macOS' build
